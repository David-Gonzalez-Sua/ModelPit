# hackCU12

## Usage
execute `server.py` inside of backend directory and connect to the local server at `http://localhost:8000`

Model configuration is done with environmental variables accessible to the server. EX:

```Bash
export GEMINI_KEY="<key>"
export OPENAI_API_KEY=""<key>"
```

Ollama can be pointed at a server or connects to localhost by default.
